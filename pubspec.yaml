name: flutter_clean_architecture_sample
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  connectivity_plus: 2.2.0
  cupertino_icons: 1.0.2
  dartz: 0.10.1
  data_connection_checker: 0.3.4
  equatable: 2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: 8.1.1
  get_it: 7.2.0
  http: 0.13.5
  shared_preferences: 2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  mockito: 5.2.0

flutter:
  uses-material-design: true
